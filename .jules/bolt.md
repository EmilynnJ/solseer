## 2026-09-01 - Prevent Memory Bloat in Drizzle Pagination with Correlated Subqueries
**Learning:** Using `LEFT JOIN` and `GROUP BY` to count related records (e.g., counting comments for posts, or reviews for readers) causes significant memory bloat and performance degradation during pagination due to the database returning a row for every joined record before grouping them.
**Action:** Replace the `LEFT JOIN` + `GROUP BY` pattern with a correlated subquery using `sql<number>'(select count(*)::int from child_table where parentId = ${parent_table.id})'` to significantly improve query performance and reduce memory usage in Drizzle ORM/PostgreSQL.## 2024-05-18 - Concurrent Secret Resolution in Cloudflare Worker
**Learning:** Sequential `await` in loops (`for...of`) when fetching remote bindings (like Secrets Store) is a performance bottleneck. Because JavaScript executes asynchronously within a single thread, mutating an object across parallel promise resolutions is safe and won't introduce race conditions.
**Action:** When fetching multiple independent secrets or remote bindings, always map them into an array of Promises and resolve them concurrently using `Promise.all` to significantly reduce latency.
