## 2026-09-01 - Prevent Memory Bloat in Drizzle Pagination with Correlated Subqueries
**Learning:** Using `LEFT JOIN` and `GROUP BY` to count related records (e.g., counting comments for posts, or reviews for readers) causes significant memory bloat and performance degradation during pagination due to the database returning a row for every joined record before grouping them.
**Action:** Replace the `LEFT JOIN` + `GROUP BY` pattern with a correlated subquery using `sql<number>'(select count(*)::int from child_table where parentId = ${parent_table.id})'` to significantly improve query performance and reduce memory usage in Drizzle ORM/PostgreSQL.## 2026-09-26 - Image Loading Performance
**Learning:** Adding `loading="lazy"` to lists of off-screen images saves bandwidth and reduces main thread contention during initial load, while adding `fetchPriority="high"` to hero images (like main profile pictures) instructs the browser to prioritize them, improving the Largest Contentful Paint (LCP). Additionally, combining these with `decoding="async"` prevents image decoding from blocking the main thread.
**Action:** Consistently apply `loading="lazy"` and `decoding="async"` to images below the fold or in lists, and apply `fetchPriority="high"` and `decoding="async"` to above-the-fold hero images. Ensure standard JavaScript block comments `/* */` are used when commenting inside JSX element attributes.
