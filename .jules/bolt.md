## 2026-09-01 - Prevent Memory Bloat in Drizzle Pagination with Correlated Subqueries
**Learning:** Using `LEFT JOIN` and `GROUP BY` to count related records (e.g., counting comments for posts, or reviews for readers) causes significant memory bloat and performance degradation during pagination due to the database returning a row for every joined record before grouping them.
**Action:** Replace the `LEFT JOIN` + `GROUP BY` pattern with a correlated subquery using `sql<number>'(select count(*)::int from child_table where parentId = ${parent_table.id})'` to significantly improve query performance and reduce memory usage in Drizzle ORM/PostgreSQL.## 2026-09-18 - Optimize Image Loading for Performance
**Learning:** Eagerly loading off-screen images or failing to prioritize above-the-fold hero images hurts initial page load performance and metrics like Largest Contentful Paint (LCP).
**Action:** Always add `loading="lazy"` to images that render below the fold, especially in lists. For critical above-the-fold hero images, add `fetchPriority="high"` to prioritize their loading. Note: when adding comments to JSX attributes, use standard JavaScript block comments `/* comment */` to avoid syntax errors.
