## 2026-08-16 - Modal Keyboard Navigation & Dismissal
**Learning:** React Modal dialogs should listen for the `Escape` key to provide intuitive keyboard navigation and modal dismissal across all dialog overlays.
**Action:** Always include a `keydown` listener for `Escape` in reusable Modal components and add visual title tooltips to icon-only close buttons.
